apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MySQL HeatWave connection resources
# Note: MySQL database itself is managed by OCI (terraform/05-mysql-heatwave)
# These are just K8s resources for apps to connect to HeatWave

resources:
  - mysql.yaml
